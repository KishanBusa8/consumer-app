class FormValidator{
  static phoneNumberValidation(value){
                  
                    // if (value!.trim().isEmpty) {
                    //   return 'Enter mobile';
                    // } else if (value.trim().length > 9  && '${value[0]}' == '4') {
                    //   return 'Invalid Mobile Number';
                    // } else if (value.trim().length < 9  && '${value[0]}' == '4') {
                    //   return 'Invalid Mobile Number';
                    // }else if ('${value[0]}' != '4' &&value.trim().length < 9 ) {
                    //   return "Invalid Mobile Number\nPlease enter valid mobile number starting with 4";
                    // }
                    //
                    // else if ('${value[0]}' != '4') {
                    //   return "Invalid Mobile Number\nPlease enter valid mobile number starting with 4";
                    // }else if ('${value[0]}' != '4' &&value.trim().length > 9 ) {
                    //   return "Invalid Mobile Number\nPlease enter valid mobile number starting with 4";
                    // }
                    return null;
                  }
}